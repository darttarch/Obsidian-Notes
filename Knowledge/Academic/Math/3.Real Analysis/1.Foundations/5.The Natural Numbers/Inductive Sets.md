>[!def]
>A se $N$ is inductive if:
$\bullet$  $0\in N$.
>$\bullet$ if $n\in N$ then $n+1\in N$.

basically of a set contains zero and each number have a successor in the same set, then it's inductive.